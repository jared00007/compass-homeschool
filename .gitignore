*.db
*.db-journal
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store
.env
# No trailing slash -- setup-cloud-backups.sh replaces this with a symlink
# into Google Drive, and a trailing-slash pattern only matches real
# directories, not symlinks, so it stopped being ignored once symlinked.
backups
.claude/
.agents/
